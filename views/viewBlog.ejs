<!DOCTYPE html>
<html lang="en">
<head>
<%- include('./partials/head') %>
<title><%= blog.title %> - BlogX</title>
<style>
    .blog-header {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 3rem 0;
        margin-bottom: 2rem;
    }
    
    .blog-cover {
        width: 100%;
        max-height: 400px;
        object-fit: cover;
        border-radius: 8px;
        margin-bottom: 2rem;
    }
    
    .blog-content {
        font-size: 1.1rem;
        line-height: 1.8;
        color: #333;
    }
    
    .blog-meta {
        background: #f8f9fa;
        padding: 1rem;
        border-radius: 8px;
        margin-bottom: 2rem;
        border-left: 4px solid #007bff;
    }
    
    .blog-body {
        white-space: pre-wrap;
        margin-bottom: 2rem;
    }
</style>
</head>
<body>
<%- include('./partials/nav') %>

<div class="blog-header">
    <div class="container">
        <h1 class="display-4"><%= blog.title %></h1>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <div class="blog-meta">
                <div class="row">
                    <div class="col-md-6">
                        <strong>Author:</strong> <%= blog.createdBy?.fullName || 'Anonymous' %>
                    </div>
                    <div class="col-md-6 text-md-end">
                        <strong>Published:</strong> <%= new Date(blog.createdAt).toLocaleDateString('en-US', { 
                            year: 'numeric', 
                            month: 'long', 
                            day: 'numeric' 
                        }) %>
                    </div>
                </div>
            </div>
            
            <% if (blog.coverImageURL) { %>
                <img src="<%= blog.coverImageURL %>" alt="<%= blog.title %>" class="blog-cover">
            <% } %>
            
            <div class="blog-content">
                <div class="blog-body"><%= blog.body %></div>
            </div>
            
            <div class="text-center mt-4">
                <a href="/" class="btn btn-outline-primary">← Back to All Blogs</a>
            </div>
        </div>
    </div>
</div>

<%- include('./partials/scripts') %>
</body>
</html>